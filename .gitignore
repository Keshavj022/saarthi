# ---- Secrets / environment ----
.env

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# ---- Data / generated artifacts ----
data/app.db
data/outputs/*
!data/outputs/.gitkeep
# Keep the headline benchmark artifacts in the repo (reproducible via
# scripts/run_benchmark.py, but committed so the result is always visible).
!data/outputs/benchmark.png
!data/outputs/benchmark.json
data/videos/*
!data/videos/.gitkeep

# ---- SUMO run artifacts (generated; the .net.xml is built from inputs) ----
*.tripinfo.xml
*.summary.xml
*.queue.xml
sumo.log
*.tmp

# ---- Model weights (perception, Phase 3+) ----
*.pt
*.pth
*.onnx

# ---- Tooling runtime state ----
.claude-flow/

# ---- OS ----
.DS_Store
